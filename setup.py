from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Investigating the feasibility of deriving TTC values for an inhalation route of entry.',
    author='patlewig',
    license='',
)
